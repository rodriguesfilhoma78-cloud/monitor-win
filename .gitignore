__pycache__/
*.pyc
.venv/
*.log

# dados de runtime (regravados pelo VBA/servidor durante o pregao)
dados_*.csv
*.db
niveis.json
ranking_acum.csv

# backups locais (nomes tipo "win_history.db.bak_..." nao batem com *.db/*.log
# porque a extensao real fica no meio do nome, nao no final - excluir a pasta
# inteira e mais simples e seguro que tentar cobrir cada padrao de sufixo)
_backups/
